apiVersion: v1
kind: Service
metadata:
  name: order-service-worker-metrics
spec:
  type: ClusterIP
  selector:
    app: order-service-worker
  ports:
    - name: metrics
      port: 9000
      targetPort: 9000