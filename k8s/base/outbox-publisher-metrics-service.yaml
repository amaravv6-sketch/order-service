apiVersion: v1
kind: Service
metadata:
  name: order-service-outbox-publisher-metrics
spec:
  type: ClusterIP
  selector:
    app: order-service-outbox-publisher
  ports:
    - name: metrics
      port: 9000
      targetPort: 9000