apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - configmap.yaml
  - secret.yaml
  - deployment.yaml
  - service.yaml

  - postgres-secret.yaml
  - postgres-service.yaml
  - postgres-statefulset.yaml

  - redis-service.yaml
  - redis-deployment.yaml

  - kafka-headless-service.yaml
  - kafka-service.yaml
  - kafka-statefulset.yaml

  - outbox-publisher-deployment.yaml
  - order-worker-deployment.yaml

  - outbox-publisher-metrics-service.yaml
  - order-worker-metrics-service.yaml